name: Python CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
  schedule: # запуск по расписанию каждый день в 01:00 UTC
    - cron: '0 1 * * *'

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Забираем код из репозитория
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Устанавливаем нужную версию Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # 3️⃣ Устанавливаем зависимости из requirements.txt
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 4️⃣ Скачиваем браузеры для Playwright + системные зависимости
      - name: Install Playwright Browsers
        run: |
          python -m playwright install --with-deps

      # 5️⃣ Проверяем стиль кода с помощью Black
      - name: Run linters
        run: |
          black --check .

      # 6️⃣ Запускаем тесты через pytest и сохраняем результаты для Allure
      - name: Run tests with pytest
        run: |
          pytest --junitxml=report.xml --alluredir=allure-results

      # 7️⃣ Загружаем результаты тестов как артефакты
      - name: Upload test results
        uses: actions/upload-artifact@v4
        with:
          name: pytest-results
          path: |
            report.xml
            allure-results/

      # 🔹 новый шаг — скачиваем и устанавливаем allure-cli
      - name: Install Allure CLI
        run: |
          wget https://github.com/allure-framework/allure2/releases/download/2.29.0/allure-2.29.0.zip
          unzip allure-2.29.0.zip -d /tmp/allure

      # 🔹 новый шаг — генерируем красивый HTML отчёт
      - name: Generate Allure Report
        run: |
          /tmp/allure/allure-2.29.0/bin/allure generate allure-results -o allure-report --clean

      # 🔹 новый шаг — загружаем HTML отчёт как артефакт
      - name: Upload Allure Report
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report

      # 🔹 новый шаг — публикуем Allure Report в GitHub Pages
      - name: Deploy Allure Report to Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: allure-report

      # 🔹 новый шаг — отправка ссылки в Telegram
      - name: Send Telegram message
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }} # id чата/юзера
          token: ${{ secrets.TELEGRAM_TOKEN }} # токен бота
          message: "✅ Allure Report доступен: https://<user>.github.io/<repo>/"
